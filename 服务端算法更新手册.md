# BVH 算法服务代码更新手册（brss1-algorithm）

> 适用对象：负责维护 `brss1-algorithm`（BVH 算法服务）的开发者
> 最后更新：2026-09-15
> 相关文件：`/brss1.0/docker-compose.yml`、`/brss1.0/services/algorithm/`、`/brss1.0/algorithm-build-stage/`

---

## 0. 一页速查

| 你改了什么 | 要做什么 | 大概耗时 |
| --- | --- | --- |
| 只改 Python 代码（`.py`） | `git pull` → `docker compose restart algorithm` | 秒级 + 约 1 分钟启动 |
| 改了模型权重（`resources/mdm/**`） | `git pull` + `git lfs pull` → `docker compose restart algorithm` | 同上 |
| 改了 `pyproject.toml` / `uv.lock` / `Dockerfile` / 系统依赖 | 必须**重建镜像**，动了依赖还要先用构建目录自带的 uv 重新 `lock`（注意保留国内镜像配置，见第 5 节） | 几分钟到十几分钟 |
| 只改 `tests/` | 什么都不用做，测试文件不进镜像也不挂载，在本机跑就行（见第 7 节） | — |

**最容易踩的坑：`docker restart` 不会应用 compose 里的新挂载。** 挂载点是创建容器时固定的；新增或修改挂载必须重建容器（`docker compose up -d algorithm`）。挂载本身已经配好了，日常改代码用 `restart` 就够了。

---

## 1. 这次做了什么（2026-09-15）

### 1.1 改动内容

只改了一个文件：`/brss1.0/docker-compose.yml`，在 `algorithm` 服务下新增一个只读绑定挂载。

```diff
--- docker-compose.yml.bak-20260915-194611
+++ docker-compose.yml
@@ -90,6 +90,10 @@
       SSL_CERT_FILE: /etc/ssl/certs/bdcatek-ca-bundle.crt
     volumes:
       - ./services/algorithm/certs/bdcatek-ca-bundle.crt:/etc/ssl/certs/bdcatek-ca-bundle.crt:ro
+      # 源码挂载：改代码后只需 restart 容器，无需重建镜像；
+      # 镜像层在机械盘（/dev/sda1，Docker Root Dir）约 38 MB/s，源码在 NVMe 约 1.9 GB/s，
+      # MDM 内置权重在 /app/src/bvh_processing/resources/mdm 下，冷读因此快约一个数量级
+      - ./services/algorithm/src/bvh_processing/src:/app/src:ro
     networks:
       - app-network
```

映射关系：

```
宿主机 /brss1.0/services/algorithm/src/bvh_processing/src
      └── bvh_processing/            ← Python 包
          ├── api/ cli.py main.py ...
          └── resources/mdm/         ← MDM 权重（232 MB .pt + 268 MB safetensors）
                    ↓ 挂载（只读）
容器   /app/src
      └── bvh_processing/
```

### 1.2 生效方式

因为这是**新增挂载**，`docker restart` 无效，实际执行的是重建：

```bash
cd /brss1.0
docker compose up -d algorithm      # 输出：Recreate → Recreated → Started
```

### 1.3 改完验证过的事情

| 验证项 | 结果 |
| --- | --- |
| 容器状态 | `Up (healthy)` |
| 挂载是否生效 | `docker inspect` 显示 `/brss1.0/services/algorithm/src/bvh_processing/src -> /app/src (ro)` |
| 容器内 import 路径 | `bvh_processing.__file__` = `/app/src/bvh_processing/__init__.py`（走挂载，不是镜像副本） |
| 改代码是否立即可见 | 在宿主机新建 `_mount_probe.py`，容器内立刻能 import；删除后容器内也消失 |
| 权重是否可读 | 容器内（uid 10001）能正常打开 232 MB / 268 MB 两个权重文件 |
| 模型读取速度 | 38.7 MB/s → **1.7 GB/s**（约 45 倍，见第 3 节） |
| 容器内代码 vs 仓库代码 | 所有 `.py` 的 md5 完全一致，本次改动**没有引入任何行为变化** |
| HTTPS 证书 | 容器内对 `work.bdcatek.com:50180` 握手正常（TLSv1.3），MinIO 预签名下载不再报 `CERTIFICATE_VERIFY_FAILED` |

---

## 2. 为什么加个挂载就能让新代码生效

关键在于这个镜像里的包是 **可编辑安装（editable install）**。容器内 `/app/.venv/lib/python3.12/site-packages/bvh_processing.pth` 的内容只有一行：

```
/app/src
```

也就是说 `import bvh_processing` 不会去读 `site-packages` 里的副本，而是直接读 `/app/src` 目录。而 `/app/src` 现在被宿主机的工作区覆盖了，所以：

- 宿主机上的源码文件，就是容器运行时真正加载的源码；
- 改文件不需要 `docker build`，也不需要把文件 `docker cp` 进容器。

三个必须知道的细节：

1. **只读挂载**（结尾的 `:ro`）。容器不会写你的工作区。镜像里设了 `PYTHONDONTWRITEBYTECODE=1`，所以也不会生成 `__pycache__` / `.pyc`。
2. **文件可见 ≠ 代码已生效。** 挂载让新文件在容器里立即可见，但已经在跑的 Python 进程早就把旧模块加载进内存了。必须重启容器让新代码被重新 import。
3. **挂载只覆盖包目录**，不包含 `pyproject.toml`、`uv.lock`、`.venv`、`tests/`。这些变了要走重建镜像。

---

## 3. 为什么模型加载快了一个数量级

### 3.1 实测数据

对同一个 232 MB 的 MDM checkpoint 做绕过页缓存的直读（`O_DIRECT`）：

```bash
# 挂载前：读容器内镜像层的文件
docker exec brss1-algorithm sh -c 'dd if=/app/src/bvh_processing/resources/mdm/checkpoint/model000600000.pt of=/dev/null bs=1M count=200 iflag=direct 2>&1 | tail -1'
# 209715200 bytes copied, 5.42442 s, 38.7 MB/s

# 挂载后：依然读 /app/src，但底层已经是 NVMe
docker exec brss1-algorithm sh -c 'dd if=/app/src/bvh_processing/resources/mdm/checkpoint/model000600000.pt of=/dev/null bs=1M count=200 iflag=direct 2>&1 | tail -1'
# 209715200 bytes copied, 0.120873 s, 1.7 GB/s
```

### 3.2 原因

```bash
docker info | grep 'Docker Root Dir'
# Docker Root Dir: /mnt/6b95d8f5-f8bb-46c0-9289-5a06e564cd19/docker/docker

lsblk -o NAME,ROTA,SIZE,MOUNTPOINT
# sda    1   1.8T   → 机械盘（ROTA=1），Docker 镜像层在这里
# nvme0n1 0  476.9G  → NVMe 固态，/brss1.0 在这里
```

镜像层（overlay2）位于 **机械盘** 上，读 500 MB 权重只有 38 MB/s 左右；宿主机源码在 **NVMe** 上，1.7～1.9 GB/s。MDM 权重（`model000600000.pt` + `model.safetensors`，合计约 500 MB，还有 torch 加载时的随机读）因此从"几十秒"降到"一两秒"。

> 顺带一提：这个优化只影响**读取速度**，不改变任何算法行为。镜像里那份权重仍然存在，只是不再被读到。

---

## 4. 日常更新代码（最常用，只改 Python 代码时）

### 4.1 目录

**要 `git pull` 的目录就是代码仓库的克隆目录：**

```bash
/brss1.0/services/algorithm/src/bvh_processing
```

- Git 远端：`git@github.com:zhuhaoxlj/bvh_processing.git`（SSH）
- 分支：`main`，跟踪 `origin/main`
- 这个目录就是挂载到容器 `/app/src` 的那个 `src/` 的父目录

⚠️ **注意：直接 `git pull` 目前会失败**，原因是本机没有可用于该仓库的 GitHub 凭据：

```
$ git pull
git@github.com: Permission denied (publickey).
fatal: 无法读取远程仓库。
```

先按第 6 节配好凭据，再执行下面的流程。

### 4.2 标准流程

```bash
# ① 进仓库目录，拉取新代码
cd /brss1.0/services/algorithm/src/bvh_processing
git status                      # 确认工作区干净（有本地改动先 stash 或提交）
git pull

# ② 如果这次更新包含 resources/mdm 下的权重文件，补一条（权重是 Git LFS 管理的）
git lfs pull

# ③ 重启容器，让新代码生效
cd /brss1.0
docker compose restart algorithm
#   等价写法（在任意目录）：docker restart brss1-algorithm

# ④ 确认服务恢复
docker exec brss1-algorithm python -c \
  "import urllib.request; print(urllib.request.urlopen('http://127.0.0.1:9001/health', timeout=5).read().decode())"
# 期望输出：{"status":"ok"}
```

### 4.3 关于重启的几点说明

- **重启期间服务不可用，实测约 55 秒才恢复**（进程要 import torch / mujoco 等重依赖）。服务就绪前调用会返回 `Connection refused`，`docker ps` 会先显示 `(health: starting)`。轮询等待即可：

  ```bash
  for i in $(seq 1 15); do
    h=$(docker exec brss1-algorithm python -c "import urllib.request; print(urllib.request.urlopen('http://127.0.0.1:9001/health',timeout=3).read().decode())" 2>/dev/null)
    [ -n "$h" ] && { echo "ready: $h"; break; }
    echo "waiting..."; sleep 6
  done
  ```

- **`restart` 不会重新读取 compose 文件。** 挂载已经存在，所以日常重启没问题；但如果以后再改 `volumes:`、`environment:`、`command:`，必须用 `docker compose up -d algorithm` 重建容器。
- 重启 algorithm **不会**连带重启 `platform`。虽然 `platform` 里有 `depends_on: algorithm (service_healthy)`，但那只在 `platform` 自己创建时生效。algorithm 重启窗口内，`platform` 发起的算法调用会失败 —— 建议在没人跑任务时更新。
- 想避免"拉到一半的代码被在跑的进程读到"，可以先停再拉：

  ```bash
  cd /brss1.0
  docker compose stop algorithm
  cd services/algorithm/src/bvh_processing && git pull
  cd /brss1.0 && docker compose start algorithm
  ```

---

## 5. 需要重建镜像的情况

### 5.1 什么情况下必须重建

挂载只覆盖了包目录，所以下面这些改动**不会**通过重启生效，必须重建 `brss1-bvh-processing:1.0`：

- `pyproject.toml` / `uv.lock` 变化（增删依赖、改版本）
- `Dockerfile` 变化（系统包、环境变量、启动命令、健康检查）
- 需要新的 apt 系统库（如 `libgl1`、`libosmesa6` 这类）
- 改了 `.docker/uv` 里的 uv 二进制

### 5.2 重建步骤

镜像的构建上下文是 **`/brss1.0/algorithm-build-stage`**，不是 `services/algorithm`。原因是 `Dockerfile` 里写的是 `COPY src ./src`，它期望 `src/` 下**直接就是 `bvh_processing` 包**；而 `services/algorithm/src/bvh_processing/` 是一个完整的 Git 克隆（里面还嵌了一层 `src/`），直接拿它当上下文会得到 `/app/src/bvh_processing/src/...` 的错误目录结构。

`algorithm-build-stage` 就是为构建单独准备的"拍平"目录，结构和克隆里的 `src/bvh_processing/` 一一对应。

```bash
# ① 先拉最新代码（同第 4 节）
cd /brss1.0/services/algorithm/src/bvh_processing
git pull

# ② 只同步包源码（扁平结构），不要动 pyproject.toml / uv.lock，原因见 5.3
rm -rf /brss1.0/algorithm-build-stage/src/bvh_processing
cp -a src/bvh_processing /brss1.0/algorithm-build-stage/src/bvh_processing
cp README.md /brss1.0/algorithm-build-stage/

# ③ 构建镜像（同名覆盖旧镜像）
cd /brss1.0/algorithm-build-stage
docker build -t brss1-bvh-processing:1.0 .

# ④ 用新镜像重建容器（必须 up -d，不能 restart）
cd /brss1.0
docker compose up -d algorithm
```

### 5.3 构建目录里的 `pyproject.toml` / `uv.lock` 不能直接用仓库里的覆盖

`algorithm-build-stage` 里的 `pyproject.toml` 和 `uv.lock` 与 Git 仓库里的**故意不一样**：

- 仓库里的版本走官方源（`https://download.pytorch.org/whl/cu128`、官方 PyPI）；
- 构建目录里的版本额外配了两个国内镜像 —— torch/triton 走上海交大 PyTorch 镜像，其余包走阿里云 PyPI，并用 `uv lock` 重新解析，所以 `uv.lock` 里所有下载地址都指向 `mirrors.aliyun.com`。

这套配置是必需的：直接从国内连官方源大概只有 74 KB/s，而走镜像能到 27 MB/s。如果拿仓库里的 `pyproject.toml` / `uv.lock` 覆盖构建目录，构建会变得非常慢甚至超时。

镜像加速配置长这样（在 `pyproject.toml` 末尾）：

```toml
# 国内加速：torch/triton 走上海交大 pytorch 镜像（PEP503 结构）
[[tool.uv.index]]
name = "pytorch-cu128"
url = "https://mirror.sjtu.edu.cn/pytorch-wheels/cu128"
explicit = true

# 其余包走阿里云 PyPI 镜像（实测 27MB/s，官方源直连只有 74KB/s）
[[tool.uv.index]]
name = "aliyun-pypi"
url = "https://mirrors.aliyun.com/pypi/simple/"
default = true
```

目录里的 `pyproject.toml.bak-src` / `uv.lock.bak-src` 是**上一版仓库原始文件**的存档，用来做对比。

**所以：**

| 情况 | 做法 |
| --- | --- |
| 只改了 Python 代码 | 只同步 `src/`（见 5.2），`pyproject.toml` / `uv.lock` 一概不动 |
| 改了依赖（`[project].dependencies` 变了） | 按下面的步骤走，**保留两个镜像 index 配置** |

依赖变更时的正确做法：

```bash
cd /brss1.0/algorithm-build-stage

# 1. 看仓库这次改了什么依赖
diff -u pyproject.toml.bak-src /brss1.0/services/algorithm/src/bvh_processing/pyproject.toml

# 2. 把新 pyproject 里的依赖变更手动合进构建目录的 pyproject.toml，
#    【务必保留】[tool.uv.index] 下 SJTU + aliyun 两段配置

# 3. 更新存档，方便下次对比
cp /brss1.0/services/algorithm/src/bvh_processing/pyproject.toml pyproject.toml.bak-src
cp /brss1.0/services/algorithm/src/bvh_processing/uv.lock      uv.lock.bak-src

# 4. 用构建目录自带的 uv 重新解析依赖（会通过国内镜像生成新的 uv.lock）
./.docker/uv lock            # 该 uv 版本为 0.12.12，可在宿主机直接运行

# 5. 再执行 5.2 的 ③④ 构建并重建容器
```

> `Dockerfile` 里用的是 `uv sync --frozen`，它要求 `uv.lock` 与 `pyproject.toml` 严格一致 —— 改了 `pyproject.toml` 却忘了重新 `uv lock`，构建会直接报错失败。

### 5.4 构建目录的其它提醒

- `/brss1.0/algorithm-build-stage/Dockerfile` 才是当前在用的版本，它比 `services/algorithm/Dockerfile` 新，多了两处关键内容：
  - `RUN rm -rf /root/.cache/uv` —— 删掉 uv 下载缓存，**否则镜像会多出约 6.8 GB**；
  - `chown algorithm:algorithm /app /app/.venv /app/src`。
  当前正在运行的镜像（15.7 GB）仍然带着那 6.8 GB 缓存（已实测确认），说明它是用旧 Dockerfile 构建的。**下次重建后镜像体积应明显下降**，看到镜像变小是正常的，不是构建失败。
- `.docker/uv`（49 MB 的 uv 二进制）是 `COPY .docker/uv /usr/local/bin/uv` 需要的，只要不从零重建构建目录就不用管它。
- 镜像体积大（十几 GB）、依赖多，构建耗时较长，建议在没有任务、也不赶时间的时候做。

---

## 6. 本机 GitHub 凭据（重要，现在的阻塞点）

当前状态：`git pull` 会报 `Permission denied (publickey)`。本机 `~/.ssh/` 里只有 `id_ed25519_1panel`，该密钥没有被授权访问 `zhuhaoxlj/bvh_processing`。

三种修法，任选一种（A 或 B 是长期的，C 是临时兜底）。

### 方案 A：加一个 Deploy Key（推荐，只读即可）

```bash
# 1. 生成专用密钥
ssh-keygen -t ed25519 -C "brss1-algorithm-deploy" -f ~/.ssh/id_ed25519_brss1 -N ""

# 2. 把公钥内容贴到 GitHub：仓库 → Settings → Deploy keys → Add deploy key（只读不勾选写权限）
cat ~/.ssh/id_ed25519_brss1.pub

# 3. 配置 SSH 使用这个密钥
cat >> ~/.ssh/config <<'EOF'

Host github.com
  HostName github.com
  User git
  IdentityFile ~/.ssh/id_ed25519_brss1
  IdentitiesOnly yes
EOF
chmod 600 ~/.ssh/config

# 4. 验证（出现 "Hi ... You've successfully authenticated" 即成功）
ssh -T git@github.com
```

### 方案 B：用 HTTPS + Personal Access Token

```bash
cd /brss1.0/services/algorithm/src/bvh_processing
git remote set-url origin https://<你的TOKEN>@github.com/zhuhaoxlj/bvh_processing.git
```

> 注意：把 token 明文写进 remote URL 会留在 `.git/config` 里，注意权限（该仓库属于谁、谁能读这个文件）。也可以用 `git config --global credential.helper store` 配合 `~/.git-credentials`。

### 方案 C：兜底 —— 直接用源码包同步

历史上这个服务是用源码压缩包交付的（`/brss1.0/backups/bvh-processing-source.tar.gz`）。如果一时配不好凭据，可以继续走这条路：

```bash
# 1. 确认本地没有未提交改动，避免被覆盖
cd /brss1.0/services/algorithm/src/bvh_processing && git status

# 2. 解包并同步（--delete 会让本地与包内一致；保留 .git 和 .venv）
tar -xzf /path/to/bvh-processing-source.tar.gz -C /tmp/bvh-src
rsync -a --delete --exclude .git --exclude .venv /tmp/bvh-src/ /brss1.0/services/algorithm/src/bvh_processing/

# 3. 之后同样用 restart 生效
cd /brss1.0 && docker compose restart algorithm
```

这种方式同步完，本地 clone 的 HEAD 会落后于实际代码（`git status` 会显示一堆文件已改），下次想回到 git 流程前先 `git checkout .` 或重新 clone。

---

## 7. 跑测试

`tests/` 既不在镜像里（`.dockerignore` 排除了），也没有挂载进容器，测试在**宿主机**上跑。克隆目录里已经有一个配好的虚拟环境：

```bash
cd /brss1.0/services/algorithm/src/bvh_processing
.venv/bin/python -m pytest          # 或：uv run pytest
```

单个文件：

```bash
.venv/bin/python -m pytest tests/test_mdm_merge.py -v
```

建议的更新顺序是：**先在本机跑测试 → 通过后再 restart 容器**，因为容器里没有回滚手段，只有重启。

---

## 8. 验证与排障

### 8.1 确认挂载还在

```bash
docker inspect brss1-algorithm --format '{{range .Mounts}}{{.Source}} -> {{.Destination}} (rw={{.RW}}){{"\n"}}{{end}}'
```

期望看到：

```
/brss1.0/services/algorithm/src/bvh_processing/src -> /app/src (rw=false)
/brss1.0/services/algorithm/certs/bdcatek-ca-bundle.crt -> /etc/ssl/certs/bdcatek-ca-bundle.crt (rw=false)
```

### 8.2 确认容器读的是你的代码

```bash
docker exec brss1-algorithm python -c "import bvh_processing, bvh_processing.services.tasks as t; print(bvh_processing.__file__); print(t.__file__)"
# 期望：/app/src/bvh_processing/__init__.py 和 /app/src/bvh_processing/services/tasks.py
```

### 8.3 确认改动是否已生效

```bash
# 看日志（合并/处理任务的报错都在这里）
docker logs -f --tail=100 brss1-algorithm
# 或
cd /brss1.0 && docker compose logs -f algorithm
```

如果改了代码但行为没变，99% 是忘了重启容器。

### 8.4 常见问题

| 现象 | 原因 / 处理 |
| --- | --- |
| 改了代码，容器里没变化 | 没重启。`docker compose restart algorithm` |
| 改了 `volumes:`/`environment:` 后重启没效果 | `restart` 不读 compose 文件，要 `docker compose up -d algorithm` 重建 |
| 重建后改动丢失 | 说明改动在 `pyproject.toml`/`uv.lock`/`Dockerfile` 这些没挂载的文件里，需要按第 5 节重建镜像 |
| 容器里出现了本地的实验性改动 | 容器跑的就是宿主机工作区，包括未提交改动。`git status` 看一下，必要时 `git stash` / `git checkout .` 后重启容器 |
| 启动后几十秒内调用失败 | 正常，进程加载重依赖要时间，等 `healthy` |
| `git pull` 报 publickey 错误 | 见第 6 节 |
| 合并任务报 `CERTIFICATE_VERIFY_FAILED` | 检查 `SSL_CERT_FILE` 和证书挂载：`docker exec brss1-algorithm sh -c 'echo $SSL_CERT_FILE; ls -l /etc/ssl/certs/bdcatek-ca-bundle.crt'` |
| 重建时依赖下载特别慢 | 构建目录里的 `pyproject.toml` 国内镜像配置被覆盖了，见 5.3 |
| 改 compose 后担心语法错 | `cd /brss1.0 && docker compose config --quiet && echo OK` |

---

## 9. 回滚

### 9.1 回滚挂载改动

删掉 `docker-compose.yml` 里 `algorithm` 服务的这 4 行，然后重建容器：

```bash
cd /brss1.0
docker compose up -d algorithm
```

容器会退回到"使用镜像里烤进去的那份代码副本"的状态。注意：镜像里那份代码停留在构建时的版本（本次是 `4dbfc00`），代码回滚后模型加载也会退回到机械盘速度。

### 9.2 回滚 compose 文件本身

改动前的备份在：`/brss1.0/docker-compose.yml.bak-20260915-194611`

```bash
cd /brss1.0
cp docker-compose.yml.bak-20260915-194611 docker-compose.yml
docker compose up -d algorithm
```

---

## 附录 A：关键路径速查

| 名称 | 路径 |
| --- | --- |
| Compose 文件 | `/brss1.0/docker-compose.yml` |
| Compose 项目名 | `brss1`（定义在 `/brss1.0/.env`） |
| 代码仓库克隆（**在这里 `git pull`**） | `/brss1.0/services/algorithm/src/bvh_processing` |
| 挂载到容器的宿主机目录 | `/brss1.0/services/algorithm/src/bvh_processing/src` |
| 容器内包路径 | `/app/src/bvh_processing` |
| 镜像构建目录（仅重建时需要） | `/brss1.0/algorithm-build-stage` |
| 构建用 Dockerfile | `/brss1.0/algorithm-build-stage/Dockerfile` |
| 构建用依赖配置（含国内镜像，勿被仓库版本覆盖） | `/brss1.0/algorithm-build-stage/pyproject.toml`、`uv.lock` |
| 构建目录自带的 uv | `/brss1.0/algorithm-build-stage/.docker/uv`（0.12.12） |
| 上一版仓库依赖文件存档（用于对比） | `/brss1.0/algorithm-build-stage/pyproject.toml.bak-src`、`uv.lock.bak-src` |
| 容器内虚拟环境 | `/app/.venv`（`bvh_processing.pth` 指向 `/app/src`） |
| 容器名 / 镜像名 | `brss1-algorithm` / `brss1-bvh-processing:1.0` |
| 服务端口 | 容器内 `9001`（**未映射到宿主机**，只能从容器内部或同网络容器访问） |
| 证书挂载 | `/brss1.0/services/algorithm/certs/bdcatek-ca-bundle.crt` → 容器 `/etc/ssl/certs/bdcatek-ca-bundle.crt` |
| 改动前备份 | `/brss1.0/docker-compose.yml.bak-20260915-194611` |
| 容器日志 | `docker logs brss1-algorithm` |

## 附录 B：健康检查从哪里来

`docker-compose.yml` 的 `algorithm` 服务里**没有**写 `healthcheck`，`docker ps` 显示的 healthy 来自 **镜像自带的 HEALTHCHECK**：

```
HEALTHCHECK --interval=30s --timeout=5s --start-period=20s --retries=3 \
  CMD python -c "import urllib.request; urllib.request.urlopen('http://127.0.0.1:9001/health', timeout=3).read()"
```

`platform` 服务的 `depends_on: algorithm: condition: service_healthy` 依靠的就是它。所以如果以后重建镜像时删掉了这段 HEALTHCHECK，`platform` 的启动条件会失效。

## 附录 C：本次操作的完整命令记录（便于复核）

```bash
# 1. 修改 compose（新增挂载，见第 1 节 diff）
# 2. 校验语法
cd /brss1.0 && docker compose config --quiet

# 3. 重建容器使挂载生效
docker compose up -d algorithm

# 4. 验证
docker ps --filter name=brss1-algorithm
docker inspect brss1-algorithm --format '{{range .Mounts}}{{.Source}} -> {{.Destination}}{{"\n"}}{{end}}'
docker exec brss1-algorithm python -c "import bvh_processing; print(bvh_processing.__file__)"
docker exec brss1-algorithm sh -c 'dd if=/app/src/bvh_processing/resources/mdm/checkpoint/model000600000.pt of=/dev/null bs=1M count=200 iflag=direct 2>&1 | tail -1'
docker exec brss1-algorithm python -c "import urllib.request; print(urllib.request.urlopen('http://127.0.0.1:9001/health', timeout=5).read().decode())"

# 5. 验证 restart 路径可用（后续日常更新走这条）
cd /brss1.0 && docker compose restart algorithm
```

